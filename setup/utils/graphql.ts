import {StaticQueryDocument} from "./use-static-query";


const graphql = (query: TemplateStringsArray): StaticQueryDocument => {
    throw new Error("GraphQL has not been transpiled")
}

export default graphql;
